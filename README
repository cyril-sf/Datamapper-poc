brew update
# make sure the next command returns is version 2.0.0
brew install mongodb
mkdir -p /data/db


Post # This is somehow required for the moment so auto_migrate! would work
DataMapper.auto_migrate!
