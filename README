brew update
# make sure the next command returns is version 2.0.0
brew install mongodb
mkdir -p /data/db


require 'rubygems'
require 'data_mapper'
DataMapper.finalize
require  'dm-migrations'
DataMapper.auto_migrate!
